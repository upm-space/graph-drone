{
  "name": "npmtestpackage",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "build": "babel src -d build",
    "dev": "babel src -d build --watch",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "jest": "^21.2.1"
  },
  "babel": {
    "presets": [
      "env"
    ]
  },
  "dependencies": {
    "line-reader": "^0.4.0"
  }
}
